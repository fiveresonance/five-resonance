[project]
name = "five-resonance"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ephem==4.1.6",
    "google-generativeai==0.8.3",
]

[tool.vercel]
entrypoint = "api.index:handler"
